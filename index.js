const {
  proj,
  mapping,
  disagreement,
  document,
  root,
  sym,
  pres,
} = require('./src/core');

const { serialized, deserialized } = require('./src/serialization');

module.exports = {
  proj,
  mapping,
  disagreement,
  document,
  root,
  sym,
  pres,
  serialized,
  deserialized,
};
