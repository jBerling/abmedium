// const { num, sym, seq, root } = require('./core');

describe('The core module', () => {
  xit('syncs', () => {
    // const d1 = document('1');
    // const delta1 = d1.add(2, num(22));
    // const d2 = document('2');
    // d2.add(root, seq([1, 2]));
    // d2.add(1, sym('inc'));
    // d2.sync(delta1);
    // expect(d2.value()).toMatchObject({
    //   [root]: seq([1, 2]),
    //   1: sym('inc'),
    //   2: num(22),
    // });
  });
});
